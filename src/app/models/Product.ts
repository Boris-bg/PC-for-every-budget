export interface Product {
  id: number;
  name: string;
  price: number;
  brand: string;
  warrantyPeriod: number; // in years
  category: string;
  availability: number;
  additionalDetails: string;
}
